version: "3.8"

services:
  app:
    build: .
    # ports:
    #   - "8080:8080"
    volumes:
      - ./cmd/web/public:/cmd/web/public
      - ./db:/db

  ngrok:
    image: ngrok/ngrok:latest
    network_mode: host
    environment:
      - NGROK_AUTHTOKEN=${NGROK_AUTHTOKEN}
    command: http 8080
# DEV ONLY
#   psql:
#     image: postgres:latest
#     environment:
#       POSTGRES_DB: ${DB_DATABASE}
#       POSTGRES_USER: ${DB_USERNAME}
#       POSTGRES_PASSWORD: ${DB_PASSWORD}
#     ports:
#       - "${DB_PORT}:5432"
#     volumes:
#       - psql_volume:/var/lib/postgresql/data

# volumes:
#   psql_volume:
